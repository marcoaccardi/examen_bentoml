/venv
.venv/
/clean_data.csv

# Python
__pycache__/
*.py[cod]
.pytest_cache/

# Data + artifacts (regenerated by src/prepare_data.py and src/train_model.py)
data/raw/*.csv
data/processed/*.csv
models/*.bentomodel

# Submission artifacts (never committed, never shipped inside the archive)
*.tar
submission/

# Local-only
*.zip
